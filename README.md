# Test-JS

```
yarn init -y
yarn add express
yarn add @types/express @types/node -D
yarn add ts-node typescript
npx tsc --init
```

```
# cria repositorio local
git init

# adiciona arquivos na fila
git add .

# salvar commit
git commit -m "mensagem"

# enviar para servidor
git push

# receber atualizações do servidor
git pull